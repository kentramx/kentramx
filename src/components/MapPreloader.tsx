/**
 * MapPreloader - Componente vacío (Google Maps eliminado)
 * 
 * Este componente ya no hace nada. Se mantiene para compatibilidad con App.tsx.
 * En FASE 2 se puede reutilizar para precargar Mapbox si es necesario.
 */

const MapPreloader = () => {
  // Google Maps desactivado. No se hace ninguna precarga.
  return null;
};

export default MapPreloader;
